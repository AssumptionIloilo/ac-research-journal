query GetEditorialBoard {
  EditorialBoard {
    boardGroups {
      id
      heading
      members {
        id
        profileImage {
          url
        }
        name
        subtitle
      }
    }
  }
}

import React, { FC } from 'react';
import './logo.scss';

const Logo: FC = () => {
  return (
    <div className="logo">
      <img src="/assumption-logo.svg" alt="Assumption Logo" />
    </div>
  );
};

export default Logo;
